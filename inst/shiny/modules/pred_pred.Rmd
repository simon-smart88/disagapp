```{asis, echo = {{pred_pred_knit}}, eval = {{pred_pred_knit}}, include = {{pred_pred_knit}}}
Generate predictions from the model
```

```{r, echo = {{pred_pred_knit}}, include = {{pred_pred_knit}}}
prediction <- disaggregation::predict_model(fitted_model)

plot(prediction$prediction)
plot(prediction$field)
```
